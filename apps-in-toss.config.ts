import { defineConfig } from '@apps-in-toss/web-framework/config';

export default defineConfig({
  appName: 'daily-micro-cheer',
  brand: { primaryColor: '#FF8A65' },
  webView: {},
  permissions: [],
  webBundleDir: 'dist',
});
